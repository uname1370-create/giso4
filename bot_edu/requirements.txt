python-telegram-bot[job-queue]==20.7
python-dotenv>=1.0.0

# پشتیبانی از پروکسی SOCKS5 برای اتصال تلگرام بدون VPN
# بدون این بسته، پروکسی‌های socks5:// با خطای ImportError رد می‌شوند.
httpx[socks]>=0.25.0

# ---- ماژول «یار هوشمند شغلی» (ai_mentor) ----
# صدور گواهینامهٔ PDF + QR Code. اگر نصب نباشند، بقیهٔ ربات سالم کار می‌کند
# و فقط صدور گواهینامه با پیام خطای مناسب رد می‌شود.
fpdf2>=2.7.0
qrcode[pil]>=7.4.0

# اتصال و راست‌به‌چپ‌سازی حروف فارسی در PDF (اختیاری ولی توصیه‌شده).
# بدون این دو، متن فارسی گواهینامه خوانا هست ولی حروف به هم نمی‌چسبند.
arabic-reshaper>=3.0.0
python-bidi>=0.4.2
