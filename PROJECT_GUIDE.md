# 📘 PROJECT_GUIDE — راهنمای کلی پروژه (ربات آموزش + سایت آموزش + گیسو)

<!-- updated 2026-09-06 -->
**آخرین به‌روزرسانی:** 2026-09-06
**برنچ فعال این نشست:** `arena/01a07668-giso2`
**مستند تخصصی گیسو:** [`GISO_GUIDE.md`](GISO_GUIDE.md) ← ساختار کد، جدول‌ها، توابع و آخرین تغییرات؛ از جمله §22 دستیار سوپرادمین، §23 صفحهٔ به‌روزرسانی/ورود دوستونه و §24 اجرای `1.md`.

---

## ۱. این ریپو چیست؟

این ریپو **سه سامانهٔ مستقل** را در یک مخزن نگه می‌دارد که با `main.py` ریشه با هم بوت می‌شوند:

| # | بخش | مسیر | نقش | قانون توسعه |
|---|------|------|------|--------------|
| ۱ | **ربات آموزش صادقی** | `bot_edu/` | ربات بلهٔ آموزشی: دوره، امتیاز، ماموریت، منتور AI | 🔒 **تغییر ممنوع** مگر دستور صریح |
| ۲ | **سایت آموزش** | `web/` | وب‌اپ Flask آموزش (پورت 5000) | جدا از گیسو نگه داشته شود |
| ۳ | **گیسو صادقی** | `giso/` + `main.py` | پلتفرم زیبایی: سایت Flask (پورت 5001) + ربات بله + AI + خرید مو + فروشگاه + پنل‌ها | همهٔ توسعهٔ فعال اینجاست |

**Single Source of Truth گیسو:** `giso/data/giso.db` (سایت و ربات گیسو یک دیتابیس مشترک دارند).
**دیتابیس آموزش:** `bot_edu/data/bot.db` (جدول `giso_config` و `giso_admins` مشترک بین دو سامانه است).

---

## ۲. بوت و اجرا

### `main.py` (ریشه) — نقطهٔ ورود واحد
به‌ترتیب بالا می‌آورد:
1. **ربات آموزش** (`bot_edu/bot.py`) — همیشه
2. **سایت آموزش** (`web/app.py`) — پورت **5000**
3. **سایت گیسو** (`giso/app.py`) — پورت **5001**
4. **ربات گیسو** (`giso/bot.py`) — فقط اگر `GISO_BOT_TOKEN` در env یا `bot_token` در `giso_config` باشد

هر سرویس در working-directory خودش اجرا می‌شود تا تداخل import پیش نیاید. ری‌استارت ربات گیسو با فایل flag: `giso/data/giso-restart.flag`.

```bash
# توسعه
python main.py
# پروداکشن (فقط وب گیسو)
gunicorn giso.wsgi:application --bind 0.0.0.0:5001 --workers 2
```

### env
- فقط **یک** فایل `.env` در ریشه (لودر: `env_loader.py`؛ فایل‌های زیرشاخه override توسعه‌اند).
- حداقل‌ها: `SECRET_KEY` (بدون آن گیسو بالا نمی‌آید)، `BALE_BOT_TOKEN`/`BOT_TOKEN`، و برای آنالیز حداقل یک AI provider با مدل Vision (مدیریت اصلی providerها از پنل AI گیسو).
- کلید API هرگز در لاگ/کامیت نیاید.

---

## ۳. بخش اول — ربات آموزش (`bot_edu/`)

> راهنمای فنی کامل خودِ این بخش: `bot_edu/GUIDE.md` (دست‌نخورده نگه داشته شده).

- **نقش:** ربات بلهٔ آموزشی صادقی — ثبت‌نام با شماره، دوره‌ها و درس‌ها، امتیاز و ماموریت، پنل ادمین آموزشی، منتور هوشمند (AI).
- **فایل‌های کلیدی:**
  - `bot.py` — هستهٔ ربات آموزش
  - `core.py`, `db.py` — زیرساخت و دیتابیس (`bot_edu/data/bot.db`)
  - `handlers*.py` — هندلرهای پیام/ادمین/AI/پیام‌رسانی
  - `ai_brain.py`, `mentor_service.py`, `ai_mentor/` — منتور هوشمند
  - `giso_admin.py` — **پل مشترک با گیسو**: `get_giso_config/set_giso_config` و جدول `giso_admins` (ادمین‌های گیسو اینجا ثبت می‌شوند)
  - `phoneutil.py` — نرمال‌سازی شمارهٔ ایرانی (گیسو هم از آن استفاده می‌کند)
  - `sync_giso_admins.py` — همگام‌سازی ادمین‌ها بین دو سامانه
- **قانون:** هیچ تغییری در این پوشه بدون دستور صریح انجام نمی‌شود؛ گیسو فقط از `giso_admin.py` و `phoneutil.py` آن import می‌کند.

---

## ۴. بخش دوم — سایت آموزش (`web/`)

- **نقش:** رابط وب سامانهٔ آموزش (`web/app.py`) روی پورت 5000؛ منطق اصلی آموزش داخل `bot_edu` است و این لایه نمایش/وب آن است.
- مقالات زیبایی (`giso/content.py` → `/articles`) جزو گیسو است، نه این بخش.
- توسعهٔ گیسو نباید به این بخش وابستگی جدید اضافه کند.

---

## ۵. بخش سوم — گیسو صادقی (`giso/`) — خلاصه

> جزئیات کامل معماری، جدول‌ها، توابع و آخرین تغییرات در **`GISO_GUIDE.md`**.

**گیسو چیست:** پلتفرم هوشمند زیبایی و سلامت مو/پوست — سایت + ربات بله + AI.

**پنج سرویس اصلی:**
1. **خرید موی طبیعی** (`/hair-sale`) — تخمین قیمت، ثبت درخواست با عکس، گفتگوی زنده، مدیریت وضعیت ۶گانه توسط ادمین (سایت + ربات)
2. **آنالیز هوشمند** (`/analysis`) — تحلیل عکس مو/پوست با AI Vision، سوالات تکمیلی، گزارش نهایی، برنامه هفتگی (خروجی فقط آنلاین؛ دانلود جدا حذف شده)
3. **فروشگاه** (`/shop`) — محصولات، سبد خرید، پرداخت در محل، ایمپورت هوشمند از کانال بله، کد تخفیف، کد پیگیری
4. **مشاور هوشمند** — ویجت چت سایت (نقش‌آگاه: مهمان/کاربر/ادمین/سوپر) + مشاور داخل ربات
5. **پنل‌ها** — پنل ادمین وب (`/admin`)، پنل کاربر سایت، پنل ادمین ربات (سوپر/معمولی با کنترل دسترسی ریزدانه)

**نقشهٔ فایل‌های اصلی گیسو:**

| موضوع | فایل |
|---|---|
| ورود وب / روت‌های اصلی / ویجت | `giso/app.py` |
| ربات بلهٔ گیسو (یکپارچه — شکستن آن ممنوع) | `giso/bot.py` |
| آنالیز | `giso/analysis.py` (+ `analysis_final_override.py` پشتیبان) |
| خرید مو | `giso/hair_sale.py`, `giso/bot_hair_admin.py`, `giso/bot_reports.py` |
| فروشگاه ماژولار | `giso/shop/` (routes / logic / bot / panel) |
| AI | `giso/ai_brain.py`, `giso/ai_runtime.py`, `giso/gemini_proxy_manager.py` |
| دسترسی/مجوز | `giso/panel/authz.py`, `giso/panel/permissions.py`, `giso/bot_permissions.py`, `giso/admin_access_schema.py` |
| پنل ادمین وب | `giso/panel/` (ماژول‌ها + قالب‌ها) |
| پنل کاربر سایت | `giso/panel_user/` |
| مدل‌ها / دیتابیس | `giso/models.py` → `giso/data/giso.db` |
| تم‌ها (۲ تم قابل انتخاب: `original` ← مسیر هوشمند گیسو، `smart_assistant` ← دستیار هوشمند؛ مقادیر تاریخی به `smart_assistant` نگاشت می‌شوند) | `giso/static/css/home_original.css`, `theme_experiences.css`, `panel/modules/settings.py` |

---

## ۶. قوانین ثابت توسعه

1. کار فقط روی برنچ فعال؛ commit/push مستقیم به برنچ‌های مرجع ممنوع.
2. `bot_edu/` و `web/` تغییر نمی‌کنند (LOCKED) مگر دستور صریح و مستند.
3. **شکستن/refactor بزرگ `giso/bot.py` لغو شده و انجام نمی‌شود** — فقط فیکس موضعی (import/dispatch).
4. **هر کدِ گیسو در ماژولِ دامنهٔ خودش** زیر `giso/` نوشته می‌شود؛ نه فایل بزرگ عمومی (`utils.py`/`helpers.py` ممنوع). **مطابق `GISO_GUIDE.md` §۱۱ (قوانین فاز ۱/ماژولاری).**
5. قبل از migration دیتابیس بکاپ (`giso/data/backup/`).
6. API Key در لاگ/کامیت چاپ نشود.
7. ری‌استارت فقط از مسیر flag پروژه.
8. تست‌ها: `SECRET_KEY=... python -m pytest giso/tests/ -q` (اجرای suiteها به‌صورت جدا پایدارتر است).

> **برای جزئیات معماری/پنل/دیتابیس/راهنمای پنل کاربر** → `GISO_GUIDE.md` (§۱–§۲۴). <!-- updated 2026-09-06 --> نقشهٔ خانهٔ هر کد و راهنمای کامل پنل کاربر در §۱۱ و §۱۲؛ تحویل‌های شهریور در §۱۳–§۱۸؛ مرکز پیام و دستیار در §۲۱–§۲۲؛ اجرای `1.md` و وضعیت جاری در §۲۴.
