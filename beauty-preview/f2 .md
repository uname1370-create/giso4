سلام. فاز قبلی تایید شد. حالا وارد فاز ۲ می‌شویم.
هدف این فاز: راه‌اندازی سیستم ذخیره‌سازی داده‌های مشتریان (Leads) و ساخت رابط کاربری مدیریت نوبت‌ها در پنل ادمین.

⚠️ قوانین طلایی: طبق فایل start.md، فقط این فاز را انجام بده. کد ظاهر سایت اصلی (Wizard) را دستکاری نکن مگر برای اتصال ذخیره‌سازی.

📌 وظایف فاز ۲:

۱. راه‌اندازی دیتابیس (Database Setup):
   - از پایگاه داده Prisma با پروایدر SQLite استفاده کن (برای سادگی توسعه و بی‌نیازی از سرور جدا).
   - فایل schema.prisma را در ریشه پروژه بساز.
   - مدل داده (Model) به نام `Lead` تعریف کن با فیلدهای زیر:
     - id (String, uuid)
     - fullName (String)
     - phoneNumber (String)
     - instagramId (String, Optional)
     - selectedStyle (String) // مدل ابروی انتخابی
     - originalImageUrl (String) // URL عکس کاربر
     - resultImageUrl (String) // URL عکس خروجی AI
     - status (String, default: "new") // وضعیت: new, contacted, booked, cancelled
     - notes (String, Optional) // یادداشت مدیر
     - createdAt (DateTime, default: now())

۲. ساخت API اندپوینت‌ها:
   - `POST /api/appointments`: دریافت اطلاعات از فرم مرحله ۵ (فاز ۱) و ذخیره در دیتابیس.
   - `GET /api/admin/leads`: (نیازمند احراز هویت ادمین) دریافت لیست تمام مراجعین.
   - `PATCH /api/admin/leads/[id]`: به‌روزرسانی وضعیت (Status) یا یادداشت‌ها.
   - `DELETE /api/admin/leads/[id]`: حذف امن یک لید (برای رعایت حریم خصوصی).

۳. ذخیره‌سازی تصاویر:
   - عکس آپلودی کاربر و خروجی AI را به صورت فایل در پوشه `public/uploads/leads/[leadId]/` ذخیره کن.
   - فقط آدرس (Path) فایل‌ها را در دیتابیس نگه‌دار.

۴. توسعه پنل ادمین (`app/admin/dashboard`):
   - یک تب/صفحه جدید به نام "مدیریت مراجعین" اضافه کن.
   - جدولی طراحی کن که شامل ستون‌های: نام، شماره، مدل انتخابی، تاریخ، وضعیت باشد.
   - برای هر ردیف، یک دکمه "مشاهده جزئیات" بگذار که در یک مودال (Modal) عکس‌های قبل/بعد را نشان دهد.
   - امکان تغییر وضعیت (Dropdown: تماس گرفته شد، رزرو شد، لغو) و ذخیره یادداشت‌های مدیر را فراهم کن.
   - دکمه "تماس واتساپ مستقیم" برای هر مراجع بگذار (لینک به `wa.me/[شماره]`).

۵. حریم خصوصی (Privacy):
   - در فرم مرحله ۵ سایت اصلی، یک متن کوچک اضافه کن: "اطلاعات و عکس شما به صورت محرمانه نزد مرکز عسل رجبی محفوظ است."

پس از اتمام کار، فایل start.md را آپدیت کن (تیک فاز ۲) و گزارش دقیق بده. منتظر تایید من بمان.
سلام. فاز ۲ تایید شد. حالا وارد فاز ۳ می‌شویم که مهم‌ترین بخش گسترش پروژه است.
هدف این فاز: تبدیل پروژه از یک ابزار "ابرو" به یک پلتفرم "چند-خدمته" (Multi-Service).

⚠️ قوانین طلایی:
1. منطق Vision Safety (Hard Composite) در پایتون که برای ابرو داریم را برای بقیه خدمات هم رعایت کن (چهره مشتری هرگز نباید تغییر کند).
2. خدمت "ریمو" منطق متفاوتی دارد: هوش مصنوعی تصویر نمی‌سازد، فقط ارزیابی می‌کند.

📌 وظایف فاز ۳:

۱. اضافه کردن انتخاب‌گر خدمت (Service Selector):
   - در فایل `app/page.tsx` بین مرحله Hero (Step 0) و آپلود (Step 1)، یک مرحله جدید اضافه کن.
   - چهار کارت شیک و لوکس نمایش داده شود:
     🔹 میکروبلیدینگ ابرو (فعال - همان جریان فعلی)
     🔹 شیدینگ لب (فعال - جریان جدید)
     🔹 خط چشم و بن‌مژه (فعال - جریان جدید)
     🔹 ریمو تخصصی (فعال - جریان ارزیابی)
   - انتخاب کاربر در State ذخیره شود و بقیه مراحل ویزارد بر اساس آن تغییر کند.

۲. توسعه Vision Engine (Python):
   - در پوشه `vision-engine/face/` دو فایل جدید بساز:
     - `lips.py`: تشخیص لندمارک‌ها و ساخت ماسک لب (Lip Masking).
     - `eyes.py`: تشخیص لندمارک‌ها و ساخت ماسک خط چشم و پلک.
   - فایل `vision-engine/processing/composite.py` را به‌روز کن تا بر اساس نوع خدمت (`service_type: eyebrows | lips | eyeliner`) ماسک مناسب را انتخاب و ترکیب کند.

۳. توسعه Prompt Engine (Style DNA):
   - در فایل `src/style-dna.ts` تکنیک‌های جدید را اضافه کن:
     - برای لب: `natural_blush`, `nude_pink`, `full_color`, `dark_neutralization`.
     - برای چشم: `lash_line_enhancement`, `classic_liner`, `smokey_shade`.
   - سوالات سلیقه‌ای (Step 3 ویزارد) را برای هر خدمت شخصی‌سازی کن.

۴. جریان اختصاصی برای "ریمو":
   - وقتی کاربر ریمو را انتخاب می‌کند، مرحله تولید تصویر (Step 4 قبلی) رد شود.
   - به جای آن، صفحه‌ای نمایش داده شود که می‌گوید: "تاتوی شما نیازمند بررسی حضوری توسط متخصص است. لطفاً برای دریافت مشاوره اختصاصی اطلاعات خود را ثبت کنید."
   - مستقیم به مرحله ۵ (فرم رزرو نوبت) هدایت شود.

۵. اطلاعات محتوایی:
   - برای هر خدمت جدید، تعریف کوتاه، ماندگاری تخمینی و مراقبت‌های عمومی را در یک فایل `src/services-content.ts` قرار بده و در ویزارد نمایش بده.

پس از اتمام کار، فایل start.md را آپدیت کن (تیک فاز ۳) و گزارش دقیق بده.
سلام. فاز ۳ تایید شد. حالا وارد فاز نهایی و هیجان‌انگیز پروژه می‌شویم.
هدف این فاز: پیاده‌سازی دستیار هوشمند (AI Assistant) که به عنوان مشاور مجازی عسل رجبی با کاربران در تعامل باشد.

⚠️ قوانین طلایی برای هوش مصنوعی مکالمه:
1. لحن باید صمیمی، زنانه، حرفه‌ای و لوکس باشد.
2. هرگز قیمت قطعی اعلام نکند.
3. هرگز تشخیص پزشکی ندهد و در موارد حساس (بیماری، بارداری) کاربر را به مشاور انسانی ارجاع دهد.
4. پاسخ‌ها کوتاه و ساختارمند باشند (نه پاراگراف‌های طولانی).

📌 وظایف فاز ۴:

۱. ساخت ویجت چت (Chat Widget UI):
   - یک کامپوننت شناور (Floating) در گوشه پایین سمت چپ تمام صفحات سایت اضافه کن.
   - آواتار دستیار: از عکس پروفایل عسل رجبی (که در Admin آپلود می‌شود) استفاده کن.
   - نام دستیار: "غزل - مشاور هوشمند مرکز عسل رجبی".
   - کلیک روی دکمه، پنجره چت مینیمال و لوکس (دارک/طلایی) باز کند.

۲. ساخت اندپوینت چت (`POST /api/chat`):
   - از OpenAI SDK موجود در پروژه استفاده کن.
   - System Prompt اختصاصی بنویس که شامل موارد زیر باشد:
     - شخصیت و لحن غزل.
     - لیست ۴ خدمت با توضیحات (از فایل `services-content.ts`).
     - محدودیت‌ها و خطوط قرمز.
     - قابلیت درک نیت کاربر (Intent Detection) برای هدایت به رزرو یا پیش‌نمایش.

۳. مدیریت حافظه مکالمه (Context Memory):
   - از React State یا LocalStorage برای نگه داشتن ۱۰ پیام آخر استفاده کن.
   - در هر درخواست، تاریخچه به همراه پیام جدید به API ارسال شود.

۴. اقدامات هوشمند (Smart Actions):
   دستیار باید بتواند این کارها را از داخل چت انجام دهد:
   - وقتی کاربر گفت "می‌خوام ابروم رو تست کنم" → دکمه‌ای در چت ظاهر شود که مستقیم به ویزارد آپلود عکس هدایت کند.
   - وقتی کاربر گفت "می‌خوام نوبت بگیرم" → فرم رزرو مینیمال داخل چت باز شود.
   - وقتی کاربر سوال پزشکی جدی پرسید → دکمه "تماس مستقیم با مشاور انسانی (واتساپ)" نمایش داده شود.

۵. تنظیمات پنل مدیریت:
   - در پنل ادمین بخشی به نام "تنظیمات دستیار هوشمند" اضافه کن.
   - قابلیت‌ها: 
     🔹 فعال/غیرفعال کردن کلی چت‌بات
     🔹 ویرایش پیام خوشامدگویی اولیه
     🔹 مشاهده تاریخچه مکالمات کاربران با چت‌بات (Chat Logs)
     🔹 مشاهده لیدهای تولید شده مستقیم از داخل چت.

۶. اتوماسیون یادآوری:
   - یک اسکریپت CRON ساده در `scripts/reminder.mjs` بساز که هر روز صبح لیدهای رزرو شده فردا را برای مدیر خلاصه کند (فعلاً فقط `console.log`).

پس از اتمام کار، فایل start.md را آپدیت کن (تیک فاز ۴) و گزارش دهی نهایی از کل پروژه (Overall Project Status) به من بده. پروژه با تکمیل این فاز به نسخه ۱.۰ می‌رسد!

شروع کن!
